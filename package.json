{
  "scripts": {
    "watch": "esbuild --bundle --platform=node --external:esbuild --outfile=tmp/watch.js --log-level=error scripts/watch.ts && node tmp/watch.js"
  },
  "devDependencies": {
    "@types/node": "^17.0.10",
    "esbuild": "^0.14.13",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "obs-websocket-js": "^5.0.0-alpha.5"
  }
}
